import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_playground/app/modules/login_tinder/login_tinder_store.dart';
 
void main() {
  // ignore: unused_local_variable
  late LoginTinderStore store;

  setUpAll(() {
    store = LoginTinderStore();
  });

  test('increment count', () async {
/*     expect(store.value, equals(0));
    store.increment();
    expect(store.value, equals(1)); */
  });
}